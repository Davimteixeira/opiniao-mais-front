name: Deploy Back-End Opiniao Mais

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_PORT: ${{ secrets.SSH_PORT }}
      SSH_USER: ${{ secrets.SSH_USER }}
      SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      DEPLOY_PATH: "/home/deploy/apps/opiniao-mais-back"
      ENV_PATH: "/home/deploy/.envs/opiniao-mais-back.env"

    steps:
      - name: 🔑 Configurar SSH para acessar VPS
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p $SSH_PORT $SSH_HOST >> ~/.ssh/known_hosts

      - name: 📥 Clonar o código do repositório
        uses: actions/checkout@v3

      - name: 🚀 Criar diretório remoto e corrigir permissões
        run: |
          ssh -i ~/.ssh/id_rsa -p $SSH_PORT $SSH_USER@$SSH_HOST "mkdir -p ${DEPLOY_PATH} && sudo chown -R ${SSH_USER}:${SSH_USER} ${DEPLOY_PATH} && sudo chmod -R 755 ${DEPLOY_PATH}"

      - name: 🚀 Enviar código atualizado via rsync
        run: |
          rsync -avz --delete -e "ssh -i ~/.ssh/id_rsa -p $SSH_PORT" \
            --exclude="static/" \
            --exclude="configuration/gunicorn/logs/" \
            --exclude=".git/" \
            . $SSH_USER@$SSH_HOST:$DEPLOY_PATH

      - name: 🏗️ Rodar o deploy via SSH na VPS
        run: |
          ssh -i ~/.ssh/id_rsa -p $SSH_PORT $SSH_USER@$SSH_HOST << EOF
            set -e
            cd $DEPLOY_PATH

            echo "🔗 Linkando variáveis de ambiente..."
            if [ -f "$ENV_PATH" ]; then
              ln -sf $ENV_PATH $DEPLOY_PATH/.env
              echo "✅ Variáveis de ambiente linkadas com sucesso!"
            else
              echo "❌ ERRO: O arquivo .env não foi encontrado em $ENV_PATH"
              exit 1
            fi

            echo "🔄 Corrigindo permissões..."
            sudo chown -R $SSH_USER:$SSH_USER $DEPLOY_PATH
            sudo chmod -R 755 $DEPLOY_PATH

            echo "🛑 Parando containers antigos..."
            docker compose down || docker-compose down || echo "⚠️ Nenhum container rodando."

            echo "🚀 Construindo e iniciando containers..."
            docker compose up --build -d || docker-compose up --build -d

            echo "✅ Deploy finalizado com sucesso!"
          EOF
